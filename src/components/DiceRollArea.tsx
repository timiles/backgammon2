import * as React from 'react';
import { Text, View } from 'react-native';
import styles from '../styles';

export default function DiceRollArea() {
  return (
    <View style={styles.diceRollArea}>
      <Text>TODO: dice roll area</Text>
    </View>
  );
}
